// Create a program to swap values of two variables.

#include <stdio.h>

int main()
{
    int a, b;
    printf("Enter value of a: ");
    scanf("%d", &b);
    printf("Enter value of b: ");
    scanf("%d", &a);
    printf("\nValue of a >>%d \nValue of b >>%d", a, b);
}