#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    // char a[20] = {"Hello world"};
    // char b[20] = {"this is maharshi"};
    // char c[40];
    // printf("a--- %s\n", a);
    // printf("b--- %s\n", b);
    // strcat(a, b);
    // printf("a--- %s\n", a);
    // printf("b--- %s\n", b);

    // char a[20] = {"Hello world"};
    // int lenth = strlen(a);
    // printf("%lu", lenth);

    // char a[20] = {"Hello world"};
    // char b[20] = {"this is maharshi"};
    // int result = strcmp(a, b);
    // printf("%d", result);

    // char str[20] = {"www.hello.com"};
    // char letter = 'h';
    // char *res;
    // res = strchr(str, letter);
    // printf("%s", res);

    // char a[50] = {"hello this is maharshi patel, here i am "};
    // char str[10] = {"is m"};
    // char *str2;
    // str2 = strstr(a, str);
    // printf("%s", str2);

    // char a[10] = {"6jj78"};
    // for (int i = 0; i < strlen(a); i++)
    // {
    //     if (isalpha(a[i]) != 0)
    //     {
    //         printf("%c--Alphabet is here\n", a[i]);
    //     }

    //     if (isdigit(a[i]) != 0)
    //     {
    //         printf("%c--Digit is present\n", a[i]);
    //     }
    // }

    // if ()
    // {
    //     printf("yes there is an alphabet ");
    // }
}